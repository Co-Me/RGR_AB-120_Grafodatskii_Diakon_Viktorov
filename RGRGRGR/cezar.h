#pragma once
#include <vector>
#include <string>

using namespace std;
namespace N
{
    class Cezar
    {

    public:
        string cezar(string);
        string decezar(string);
        string textParse(string);
    };

}